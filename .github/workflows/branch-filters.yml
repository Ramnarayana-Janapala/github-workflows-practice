name: Branch Specific Triggers
on:
  push:
    branches:
        - main
        - develop
    pull_request:
        branches: [main]
jobs:
  filters:
    runs-on: ubuntu-latest
    steps:
      - run: |
            echo "Running on branch : ${{ github.ref }}"
            echo "Event : ${{ github.event_name }}"
        
